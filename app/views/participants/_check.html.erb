<%= form_with model: @participant do |form| %>
  <%= form.fields_for :active do |active| %>
    <p>
      <%= form.check_box :active %>
      <%= form.label :active, "Active" %>
    </p>
  <% end %>

  <%= form.submit %>
<% end %>