<%= form_with(model: game) do |form| %>
  <% if game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :game_date, style: "display: block" %>
    <%= form.date_select :game_date %>
    <%= form.collection_check_boxes :player_ids, Player.all, :id, :name, include_hidden: false %>
    <%#= form.collection_check_boxes :,  Snack.all, :id, :name,   include_hidden: false %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<!--TODO create here a form to select active snacks but only to change their status, not to save it in game db-->

<%= form_with(model: snack) do |form| %>
  <%= form.collection_check_boxes :active, Snack.all, :id, :name %>
  <%= form.submit %>
<% end %>