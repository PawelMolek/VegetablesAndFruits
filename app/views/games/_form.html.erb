<%= form_with(model: game) do |form| %>
  <%= form.label :game_date %>
  <%= form.date_select :game_date %>
  <%= form.collection_check_boxes :participant_ids, Participant.all, :id, :name %>
  <%= form.submit %>
<% end %>
